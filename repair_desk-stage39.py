# === Stage 39: Добавь документационную строку с описанием сценариев использования ===
# Project: RepairDesk
USAGE_SCENARIOS = """
# Сценарии использования RepairDesk

## 1. Регистрация и авторизация
- Пользователь регистрируется через `register_user()`, указывая email, имя и пароль.
- Авторизация через `login_user()` с последующим получением сессии.

## 2. Управление устройствами
- Создание устройства через `create_device()` с моделью, серийным номером и статусом.
- Просмотр списка устройств по статусу или фильтру через `get_devices()`.
- Обновление статуса устройства через `update_device_status()`.

## 3. Управление заявками (tickets)
- Создание заявки через `create_ticket()` с привязкой к устройству и клиенту.
- Просмотр всех заявок через `get_tickets()`.
- Обновление статуса заявки через `update_ticket_status()`.
- Закрытие заявки через `close_ticket()`.

## 4. Диагностика и запчасти
- Проведение диагностики через `add_diagnosis()` с описанием и стоимостью.
- Добавление запчасти через `add_part()` с названием, количеством и ценой.
- Просмотр истории диагностики через `get_diagnosis()`.
- Просмотр списка добавленных запчастей через `get_parts()`.

## 5. Стоимость и отчётность
- Получение итоговой стоимости ремонта через `get_total_cost()`.
- Экспорт отчёта по заявкам через `export_report()`.
"""
